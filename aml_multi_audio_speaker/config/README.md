├── config/                   # ✅ 核心修改 ②：【统一配置文件+产品专属配置段】，新增产品配置总开关，原有配置全部保留

│   ├── audio_config.json     # 新增：高/中/低/低音炮 4类产品的音频参数段（采样率/声道/解码能力）音频底层核心参数，与src/audio_core模块强绑定，包含采样率、声道数、硬件解码、缓冲区、防爆音 / 消噪参数，所有数值适配 AML SOC 硬件解码能力，决定音质的核心配置，无不合理参数。

│   ├── play_config.json      # 新增：不同产品的音效开关（杜比/DTS/声场模式）,与src/play_ctrl模块强绑定，包含声场模式、音效开关、播放策略、断电续播、蓝牙断连处理，产品体验类核心配置，所有策略均为家庭影院 Soundbar 行业标准，比如蓝牙断连自动切 U 盘音源。

│   ├── source_config.json    # 新增：不同产品的音源列表（高端全音源，低端仅蓝牙+U盘）,与src/audio_source模块强绑定，Soundbar 核心配置之一，音源优先级是家庭影院的硬性标准（HDMI ARC > 光纤 > 蓝牙 > U 盘 > AUX > WIFI），包含音源开关、切换策略、超时配置，无缝切换无爆音，所有逻辑适配 AML SOC 音源管理能力。

│   ├── key_config.json       # 新增：不同产品的按键映射（高端多按键，低端仅播放/音量）,与src/peripheral/key_irq.c模块强绑定，适配 MCU 上报的按键值，包含短按 / 长按映射、按键防抖、功能绑定，严格对应 4 类产品的外设按键数量，高端全按键、低端仅基础按键、低音炮仅配对键，数值为工业级防抖最优值（20ms）。

│   ├── eq_preset.json        # 新增：产品分级EQ（高端全预设，低端仅2个基础EQ）,与src/sound_effects模块强绑定，音质差异化核心配置，频率点为行业标准（20Hz~20000Hz），增益范围 ±12dB（AML SOC 硬件 EQ 最优范围），高端 4 类专业 EQ、中端 2 类、低端 2 类基础 EQ、低音炮仅低音增益曲线，所有参数为实测最优值，无刺耳 / 闷音问题。

│   ├── hdmi_arc_config.json  # 不变（宏控编译，低端不加载）,与src/hdmi_arc模块强绑定，高端 / 中端 Soundbar 专属，低端 / 低音炮无需加载，参数为 AML SOC 原生 HDMI ARC 驱动最优值，包含 CEC 协议、音频编码、声道映射、自动回传，工业级必备的重连 / 超时参数，解决电视联动卡顿问题。

│   ├── spdif_config.json     # 不变（宏控编译，低端不加载）,与src/spdif_optical模块强绑定，高端 / 中端 Soundbar 专属，低端 / 低音炮无需加载，适配 AML SOC SPDIF 硬件解码，包含采样率、编码格式、静音检测，解决光纤音频爆音 / 断音问题，参数为行业标准值。

│   ├── subwoofer_config.json # 不变（宏控编译，仅低音炮/高端加载）,与src/subwoofer_comm模块强绑定，高端 Soundbar + 独立低音炮专属，中端 / 低端无需加载，核心是蓝牙配对 + 音量同步 + 低音功放参数，解决低音炮断连 / 音量不同步问题，所有参数为 AML 蓝牙 MESH 最优值，量产必备。

│   └── product_cfg.json      # ✅ 新增：【产品核心配置总文件】，定义4类产品的功能开关、硬件能力、模块启用清单,产品功能总开关，定义4类产品的硬 件能力、模块启用、功能开关，代码层首要加载的配置文件，所有模块的宏控逻辑均参考此配置，工业级必备，无它则分级适配失效。

